import { Inter } from 'next/font/google';

export const interBold = Inter({
  subsets: ['latin'],
  weight: '700',
  variable: '--ff-heading',
  display: 'swap',
});
export const inter = Inter({
  subsets: ['latin'],
  weight: '400',
  variable: '--ff-body',
  display: 'swap',
});
